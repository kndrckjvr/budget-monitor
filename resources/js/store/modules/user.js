// initial state
export const state = {
    user: {
        token: null,
        id: null,
    }
}

// getters
export const getters = {
    user: state => state.user
}

// actions
export const actions = {}

// mutations
export const mutations = {}